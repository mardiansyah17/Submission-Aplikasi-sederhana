<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SubmisionSimpleAndroidApp" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    </style>

    <style name="Theme.SubmisionSimpleAndroidApp" parent="Base.Theme.SubmisionSimpleAndroidApp">
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="windowSplashScreenAnimationDuration">300</item>

        <item name="android:windowSplashScreenBrandingImage">@drawable/logo</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.AppCompat.NoActionBar</item>\
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>


</resources>
